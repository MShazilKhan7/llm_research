# Core
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
jupyter>=1.0.0
ipykernel>=6.0.0

# LLM clients
openai>=1.30.0          # GPT (gpt-4o-mini)
google-generativeai>=0.7.0  # Gemini
groq>=0.9.0             # Llama via Groq (free tier)

# Optional: advanced metrics
scikit-learn>=1.3.0     # for sklearn confusion matrix / classification report
seaborn>=0.13.0         # nicer heatmaps
